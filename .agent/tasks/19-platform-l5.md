# Subagent 19 - 平台管理 (Platform) 模块 L5 升级任务

## 任务背景
当前你被指派为 **Subagent 19**，负责优化 `src/features/platform` 模块。
在全站成熟度评估中，该模块得分为 **7.0 (L4)**。和 Admin 模块类似，这是一个测试密度很强（90.8% 测试源码比）但前端表现层、文档薄弱的区域。你需要针对性地进行升维手术，使其各维度全线 ≥9。

## 目标成熟度核心要求 (L5)
根据 L5 指标要求（所有维度≥9），你需要以“精细调优”的姿态，增强其文档和UI成熟度，另外把控安全与审计机制。

## 具体执行要求

### 1. D4 (文档完整性) 升级
- 目前全模块仅有 28 个 JSDoc，请对核心 Action（例如租户注册、审批流水线、平台数据修改等核心接口）补充说明。
- 在 `/** ... */` JSDoc 内需指明租户参数校验和状态翻转的边界。

### 2. D5 (UI/UX 成熟度) 分析与支持
- 该模块似乎缺失 `components` 目录，前端表现多由页面调用动作支撑。
- 请检查现有的平台路由层或使用该模块的地方，验证并重构返回状态，以保证业务出错时具备优雅的反馈（友好文案，而不是直接抛栈）。

### 3. D6 (安全规范) 与 D8 (性能)
- 该模块目前的 Zod 校验仅发现 8 处引用，稍微偏少。如有从 API 或是客户端接收任何 JSON 或 `FormData` 的 Action，务必外包一层 `createSafeAction` 或 Zod 解析来确保接口健壮。

## 约束条件
- **开发语言**：所有的交付物、注释、控制台输出必须使用 **简体中文**。
- **勿损强项**：该模块的 D3（测试覆盖）得分为满分 9，你的优化必须建立在 100% 保持当前测试用例全部平稳通过的前提下。如果重构了安全逻辑，请一并更新单测。

## 交付与验证
完成后通知进行复核。
