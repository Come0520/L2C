# 任务指示：升级 Monitoring 模块至 L5 成熟度

## 当前状态与目标
- **模块名称**：`monitoring`（监控通知）
- **当前等级**：L4 (6.6分)
- **目标等级**：L5 (全维度稳定 ≥9分)
- **短板分析**：文档 D4(5分)，性能优化 D8(5分)，功能完整 D1(6分)，UI/UX D5(6分)

## 具体任务分解

### 1. 提升性能优化 (D8 目标: 9)
作为告警管理后台配置模块，当前 0 缓存、0 分页，这是扣分大头。
- 为所有获取监控规则列表的 action 添加分页支持 (`limit` 和 `offset`)。
- 为高频读取的全局规则配置添加 `unstable_cache` 机制，降低 DB 查询负担。

### 2. 补充功能文档与 JSDoc (D4 & D1 目标: 9)
当前文档仅 11 个 JSDoc，且代码里存在 1 个 TODO。
- 解决该 TODO。
- 对告警规则对象、监控操作核心流程的 API 方法添加完善的中文 JSDoc 说明，使其在 TypeScript 的 Intllisense 表现更佳。

### 3. UI/UX 成熟度提升 (D5 目标: 9)
仅有 1 个组件，无独立页面路由。
- 检视组件的结构，确保实现了 Loading 态、Error 处理边界（如果未引入，考虑增加统一封装层）。
- 确认监控表单或报警条目的可操作性，增加明确的 Toast/反馈提示。

### 4. 巩固测试与安全体系 (Maintain D2, D3, D6)
- 目前 `monitoring` 没有任何 `any` 类型且没有 placeholder，全覆盖率为 111%，Zod 为 47，表现很强。但在重构/修改时，必须添加对应新增功能的测试，并且维持 0 `any` 和覆盖率 110%+ 的优异标准。

## 验收标准
1. `pnpm test src/features/monitoring` 测试全部稳定通过。
2. 分页和缓存机制确切地被实现，代码质量和安全合规无回退。
3. `pnpm tsc --noEmit` 与 `lint` 没有报错。
4. **必须使用中文**进行代码注释、提交记录和进度反馈。

完成上述所有修改后，使用 `verification-before-completion` 验证。
