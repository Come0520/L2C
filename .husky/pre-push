#!/bin/sh
echo "🔍 推送前全量质量检查..."
pnpm lint || { echo "❌ ESLint 检查失败，请先修复后再推送"; exit 1; }
pnpm type-check || { echo "❌ 类型检查失败，请先修复后再推送"; exit 1; }
echo "✅ 质量检查通过，允许推送"
