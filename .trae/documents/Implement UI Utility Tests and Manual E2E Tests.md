## 测试计划

### 1. UI工具函数测试
- **目标**：为所有UI工具函数编写完整的测试用例
- **范围**：
  - `src/utils/lib-utils.ts` 中的所有函数
  - `src/lib/utils.ts` 中的 `cn` 函数
- **测试类型**：单元测试
- **框架**：Vitest
- **测试用例**：
  - `cn` 函数：测试各种类名合并场景
  - `debounce` 函数：测试防抖功能
  - `formatDate` 函数：已存在测试，需确保覆盖完整
  - `formatDateTime` 函数：已存在测试，需确保覆盖完整
  - `formatCurrency` 函数：已存在测试，需确保覆盖完整
  - `truncate` 函数：已存在测试，需确保覆盖完整

### 2. 端到端手工测试
- **目标**：验证核心业务流程的完整性
- **范围**：
  - 线索管理页面（当前打开的页面）
  - 核心功能流程：
    - 线索列表展示
    - 筛选功能
    - 线索详情查看
    - 线索状态更新
    - 导入/导出功能
    - 批量操作
- **测试方法**：手工测试
- **测试环境**：开发环境（http://localhost:5173）
- **测试步骤**：
  1. 访问线索管理页面
  2. 验证页面布局和基本功能
  3. 测试各种筛选条件
  4. 查看线索详情
  5. 执行状态更新操作
  6. 测试导入/导出功能
  7. 测试批量操作

### 3. 执行计划
1. **编写UI工具函数测试**：15分钟
   - 创建 `src/lib/utils.test.ts` 测试文件
   - 完善现有测试用例
2. **运行单元测试**：5分钟
   - 执行 `npm test` 确保测试通过
3. **执行端到端手工测试**：30分钟
   - 按照测试步骤执行
   - 记录测试结果
4. **生成测试报告**：5分钟
   - 整理测试结果
   - 生成测试报告

### 4. 预期成果
- ✅ 所有UI工具函数都有完整的测试用例
- ✅ 单元测试覆盖率达到80%以上
- ✅ 核心业务流程通过手工测试
- ✅ 生成完整的测试报告