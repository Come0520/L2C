## SQL迁移文件重复问题处理计划

### 问题分析
通过检查项目中的SQL迁移文件，发现存在3对重复的迁移文件，它们的内容完全相同，但命名格式不一致：
1. `20251120_init_tables.sql` 和 `20251120000001_init_tables.sql`
2. `20251123_rls_init.sql` 和 `20251123000001_rls_init.sql`
3. `20251128_l2c_business_process.sql` 和 `20251128000001_l2c_business_process.sql`

### 解决方案
根据迁移文件的命名规范，应该使用完整的时间戳格式 `YYYYMMDDHHMMSS_filename.sql`，因此需要删除没有时间戳的重复文件。

### 实施步骤
1. **删除重复文件**：
   - 删除 `/supabase/migrations/20251120_init_tables.sql`（保留带时间戳的版本）
   - 删除 `/supabase/migrations/20251123_rls_init.sql`（保留带时间戳的版本）
   - 删除 `/supabase/migrations/20251128_l2c_business_process.sql`（保留带时间戳的版本）

2. **验证迁移文件**：
   - 检查所有迁移文件是否按照正确的命名格式命名
   - 确保迁移文件的执行顺序正确
   - 确认最新的迁移文件 `20251203000009_enhance_leads_for_26_status.sql` 正常

3. **检查迁移文件内容**：
   - 确保所有迁移文件的SQL语法正确
   - 检查迁移文件之间的依赖关系是否合理
   - 确认没有遗漏的迁移步骤

### 预期结果
- 消除所有重复的迁移文件
- 所有迁移文件都使用统一的命名格式
- 迁移文件可以正常执行，不会出现冲突
- 数据库迁移过程顺利完成

### 注意事项
- 在删除文件之前，再次确认重复文件的内容完全相同
- 确保迁移文件的执行顺序不会因为删除操作而受到影响
- 在执行迁移之前，建议先备份数据库