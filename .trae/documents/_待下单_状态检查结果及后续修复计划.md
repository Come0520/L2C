## 检查结果

通过对代码库的全面检查，我发现"待下单"状态相关的实现基本完整，但存在以下几个问题：

1. **状态标识不一致**
   - 文档中定义的状态标识为 `ORDER_STATUS.PENDING_ORDER`
   - 前端代码中存在多种状态标识混用：`pending-place-order`、`pending_order`、`in-production`、`in_production`
   - API 路由中的状态映射需要更新

2. **状态流转规则**
   - 缺少明确的状态流转规则定义
   - 从 `PENDING_ORDER` 到 `IN_PRODUCTION` 的流转需要验证

3. **权限控制**
   - 状态变更权限矩阵需要更新
   - 确保只有订单客服可以审核下单

## 后续修复计划

### 1. 统一状态标识
- 确保所有组件和API调用使用统一的状态标识
- 更新API路由中的状态映射，确保一致性

### 2. 完善状态流转规则
- 定义明确的状态流转规则
- 确保从 `PENDING_ORDER` 到 `IN_PRODUCTION` 的流转被允许
- 添加状态流转验证逻辑

### 3. 强化权限控制
- 更新状态变更权限矩阵
- 确保只有订单客服可以审核下单
- 添加权限验证逻辑

### 4. 完善时效控制
- 确保待下单状态的时效控制符合文档要求（48小时内完成审核）
- 添加超时提醒和处理逻辑

### 5. 测试验证
- 测试状态流转的完整性
- 测试权限控制的正确性
- 测试时效控制的有效性

## 预期效果

修复后，"待下单"状态的实现将与文档完全一致，包括：
- 统一的状态标识
- 明确的状态流转规则
- 正确的权限控制
- 符合要求的时效控制

## 实施步骤

1. 统一所有组件和API调用中的状态标识
2. 完善状态流转规则定义
3. 强化权限控制逻辑
4. 完善时效控制逻辑
5. 进行全面测试验证

通过这次修复，确保系统中的"待下单"状态实现与文档完全一致，提高系统的可维护性和一致性。