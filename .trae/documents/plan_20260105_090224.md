# L2C 项目 Lint 错误修复计划

## 📊 问题概述

**当前状态**：
- ❌ **984 个错误**（必须修复）
- ⚠️ **11650 个警告**（建议修复）

---

## 🔴 核心错误类型分析

### 1. `@typescript-eslint/no-explicit-any` —— 主要问题 ⚠️
**约 800+ 处错误**，违反项目规则 "零 any 模式"

**严重文件**（按优先级排序）：
- `src/shared/lib/server-action.ts` (2处) —— 底层库，影响全局
- `src/shared/ui/command.tsx` (1处) —— 空接口问题
- `src/shared/ui/action-confirm-dialog.tsx` (1处)
- `src/shared/ui/hover-border-gradient.tsx` (2处)
- `src/shared/providers/tenant-provider.tsx` (1处)
- `src/features/orders/__tests__/` 测试文件 (大量)
- `src/features/quotes/` 报价模块 (大量)
- `src/features/service/measurement/` 测量模块 (大量)

### 2. `react/no-unescaped-entities` —— 实体转义错误
**4 处错误**，必须修复：
```
src/features/quotes/components/quote-copilot-dialog.tsx:109
src/features/service/installation/components/submit-completion-dialog.tsx:189
```

### 3. `@typescript-eslint/no-empty-object-type` —— 空接口问题
**1 处错误**：
```
src/shared/ui/command.tsx:26
```

### 4. 未使用的变量警告 —— 清理即可
**约 40+ 处警告**，删除未使用的 import/变量

---

## 🛠️ 修复计划（分 4 个阶段）

### **第一阶段：修复 Shared 层核心错误**
1. `server-action.ts` —— 为 `createSafeAction` 和 `safeAction` 的泛型参数添加正确类型
2. `command.tsx` —— 将空接口 `interface Props {}` 改为 `Record<string, never>` 或 `object`
3. `hover-border-gradient.tsx` —— 为 `onTransitionEndCallable` 参数添加具体类型
4. `tenant-provider.tsx` —— 为 `fetchTenant` 返回值添加类型
5. `action-confirm-dialog.tsx` —— 为 `execute` 回调参数添加类型

### **第二阶段：修复计算策略层**
1. `calc-strategies/base-strategy.ts` —— 修复 `QuoteCalculationContext` 泛型
2. `calc-strategies/curtain-fabric-strategy.ts`
3. `calc-strategies/wallpaper-strategy.ts`

### **第三阶段：修复测试文件**
1. `src/features/orders/__tests__/` —— 修复 mock 类型
2. `src/features/service/measurement/__tests__/` —— 修复 mock 类型
3. 其他测试文件的 any 类型

### **第四阶段：清理警告**
1. 删除未使用的 import 语句
2. 移除无效的 eslint-disable 指令
3. 修复 JSX 中的实体转义

---

## 📝 预期产出

✅ Lint 错误数降至 **0**
✅ 警告数大幅减少（保留合理警告）
✅ 符合项目 "零 any 模式" 规范

---

## ⏱️ 预估时间

- 第一阶段（核心层）：30 分钟
- 第二阶段（策略层）：20 分钟  
- 第三阶段（测试层）：45 分钟
- 第四阶段（清理）：15 分钟

**总计：约 2 小时**

---

是否同意此修复计划？