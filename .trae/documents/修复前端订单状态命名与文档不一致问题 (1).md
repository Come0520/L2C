# 修复前端订单状态命名与文档不一致问题

## 1. 实现DELETE API端点

### 1.1 创建动态路由文件
- 创建 `/api/slides/[id]/route.ts` 文件
- 实现 DELETE 方法，支持按ID删除幻灯片

### 1.2 实现DELETE逻辑
```typescript
// DELETE /api/slides/[id] - 删除幻灯片
export async function DELETE(request: NextRequest, { params }: { params: { id: string } }) {
  try {
    const { id } = params;
    // TODO: 实现实际数据库删除逻辑
    return NextResponse.json({
      message: '幻灯片删除成功',
      slide_id: id
    });
  } catch (error) {
    return NextResponse.json(
      { error: '删除幻灯片失败' },
      { status: 500 }
    );
  }
}
```

## 2. 更新SlideCard组件

### 2.1 添加确认对话框
- 引入PaperDialog组件
- 添加状态管理确认对话框显示
- 实现确认对话框UI

### 2.2 实现删除功能
- 为删除按钮添加点击事件，显示确认对话框
- 实现确认删除逻辑，调用DELETE API
- 实现取消删除逻辑，关闭对话框

### 2.3 添加列表刷新功能
- 为SlideCard组件添加onDelete回调属性
- 在删除成功后调用onDelete回调，通知父组件刷新列表

## 3. 统一两个SlideCard组件

### 3.1 分析重复组件
- 两个SlideCard组件（editor和orders目录）功能完全相同
- 仅缩略图显示方式不同（img vs Image组件）

### 3.2 重构为共享组件
- 将SlideCard组件移动到共享目录
- 添加属性控制缩略图显示方式
- 更新所有引用该组件的地方

## 4. 测试实现

### 4.1 测试API端点
- 测试DELETE /api/slides/[id] 端点
- 验证成功和失败情况

### 4.2 测试组件功能
- 测试删除按钮显示确认对话框
- 测试确认删除流程
- 测试取消删除流程
- 测试列表刷新功能

## 5. 代码质量检查

### 5.1 遵循命名规范
- 变量命名使用camelCase
- 常量命名使用SCREAMING_SNAKE_CASE
- 函数命名使用camelCase
- 文件命名符合规范

### 5.2 代码精简
- 删除无用代码和注释
- 移除重复逻辑
- 每个函数保持单一职责

### 5.3 结构有序
- 组件结构清晰
- 导入顺序合理
- 代码组织有序

## 6. 文档更新

### 6.1 更新API文档
- 添加DELETE /api/slides/[id] 端点文档
- 更新API路由结构文档

### 6.2 更新组件文档
- 更新SlideCard组件文档
- 添加确认对话框使用示例

## 7. 提交代码

### 7.1 代码提交
- 提交实现的代码
- 提交测试结果
- 提交文档更新

### 7.2 代码审查
- 进行代码审查
- 修复审查中发现的问题
- 确保代码符合所有规范