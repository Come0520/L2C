# 修复前端订单状态命名与文档不一致问题

## 问题分析
前端 `order-status.ts` 中定义的待跟踪和跟踪中状态命名与文档不符：
- 当前实现：`PENDING_TRACKING` (待跟踪) 和 `TRACKING` (跟踪中)
- 文档预期：根据 `lead-status.ts` 中的命名规范，应该使用 `PENDING_FOLLOW_UP` (待跟踪) 和 `FOLLOWING_UP` (跟踪中)

## 修复方案
1. **更新订单状态常量定义**：
   - 修改 `src/constants/order-status.ts` 中的状态命名
   - 将 `PENDING_TRACKING` 改为 `PENDING_FOLLOW_UP`
   - 将 `TRACKING` 改为 `FOLLOWING_UP`
   - 更新对应的状态值和中文标签

2. **更新状态流转规则**：
   - 修改 `ORDER_STATUS_TRANSITIONS` 中的状态引用
   - 确保状态流转逻辑保持不变

3. **更新状态配置**：
   - 修改 `ORDER_STATUS_CONFIG` 中的状态引用
   - 确保状态样式和标签保持一致

4. **检查依赖文件**：
   - 检查所有使用这些状态的文件
   - 确保没有遗漏的引用

## 预期结果
- 前端订单状态命名与文档保持一致
- 待跟踪状态：`PENDING_FOLLOW_UP`
- 跟踪中状态：`FOLLOWING_UP`
- 所有相关代码正常运行
- 状态流转逻辑不受影响

## 实现步骤
1. 修改 `src/constants/order-status.ts` 文件中的状态定义
2. 更新状态流转规则和配置
3. 运行代码检查工具确保没有语法错误
4. 测试相关功能确保正常运行