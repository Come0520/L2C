## 问题分析

1. **现象**：访问待测量页面（`/orders/status/pending-survey`）时，顶部标签页仍然显示工作台的标签页。

2. **根本原因**：在 `dashboard-layout.tsx` 文件中，`activeGroup` 的计算逻辑存在问题：
   - 当访问 `/orders/status/pending-survey` 时，
   - 工作台组的 items 中包含 `/orders` 路径，
   - 由于 `pathname?.startsWith(`${item.href}/`)` 条件，`/orders/status/pending-survey` 会匹配 `/orders/`，
   - 因此 `exactMatchGroup` 被错误地设置为工作台组，而不是测量阶段组。

3. **具体原因**：当前逻辑在找到第一个匹配项后就返回结果，没有考虑匹配路径的长度，导致短路径匹配长路径。

## 修复方案

修改 `activeGroup` 的计算逻辑，使其：
1. 首先收集所有匹配的组和项
2. 然后选择匹配路径最长的项所属的组
3. 确保最长的匹配项被选中，避免短路径匹配长路径

## 修复步骤

1. 打开 `/Users/laichangcheng/Desktop/L2C/slideboard-frontend/src/components/layout/dashboard-layout.tsx` 文件
2. 修改 `activeGroup` 的计算逻辑
3. 测试修复效果，确保待测量页面显示正确的标签页

## 预期效果

- 访问 `/orders/status/pending-survey` 时，顶部标签页显示测量阶段的标签
- 访问 `/orders/status/draft-sign` 时，顶部标签页显示订单处理阶段的标签
- 访问 `/orders/status/installing-pending-assignment` 时，顶部标签页显示安装阶段的标签
- 其他页面的标签页显示保持不变

## 代码修改

```typescript
// Find current active group
const activeGroup = React.useMemo(() => {
  // Find all matching groups and their best matching items
  const matchingGroups = navGroups.map(group => {
    const matchingItems = group.items.filter(item => 
      pathname === item.href || pathname?.startsWith(`${item.href}/`) || pathname?.startsWith(item.href)
    );
    if (matchingItems.length === 0) return null;
    
    // Find the best (longest) matching item in this group
    const bestMatchingItem = matchingItems.reduce((best, current) => {
      return current.href.length > best.href.length ? current : best;
    }, matchingItems[0]);
    
    return { group, bestMatchingItem };
  }).filter(Boolean) as Array<{ group: typeof navGroups[0]; bestMatchingItem: typeof navGroups[0]['items'][0] }>;
  
  if (matchingGroups.length === 0) return navGroups[0];
  
  // Find the group with the longest matching item href
  const bestMatch = matchingGroups.reduce((best, current) => {
    return current.bestMatchingItem.href.length > best.bestMatchingItem.href.length ? current : best;
  }, matchingGroups[0]);
  
  return bestMatch.group;
}, [pathname, navGroups]);
```