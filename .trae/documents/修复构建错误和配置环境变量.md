## 修复计划

### 1. 创建缺失的 PaperCheckbox 组件
- **问题**：`installation-edit-form.tsx` 和 `installation-create-form.tsx` 使用了 `PaperCheckbox` 组件，但该组件不存在
- **解决方案**：根据其他 Paper UI 组件的实现模式，创建 `paper-checkbox.tsx` 组件
- **实现步骤**：
  - 在 `src/components/ui/` 目录下创建 `paper-checkbox.tsx` 文件
  - 参考 `paper-input.tsx` 的结构实现 `PaperCheckbox` 组件
  - 支持基本的 checkbox 功能，包括 label、error、helperText 等属性

### 2. 检查并添加 'use client' 标识
- **问题**：使用 React Hooks 的页面需要添加 'use client' 标识，否则会导致构建错误
- **解决方案**：搜索并检查所有使用 React Hooks 的页面组件
- **实现步骤**：
  - 搜索 `src/app/` 目录下使用了 React Hooks 但没有 'use client' 标识的文件
  - 为这些文件添加 'use client' 标识
  - 重点检查 `src/app/` 目录下的页面组件

### 3. 配置 .env.local 文件
- **问题**：需要确保 `.env.local` 文件中包含所有必要的环境变量
- **解决方案**：检查并配置所需的环境变量
- **实现步骤**：
  - 确认 `NEXT_PUBLIC_SUPABASE_URL` 和 `NEXT_PUBLIC_SUPABASE_ANON_KEY` 已正确配置
  - 根据实际需求配置 `WECHAT_*` 和 `FEISHU_*` 相关环境变量
  - 如果不需要微信和飞书集成，可以保持当前注释状态

## 预期结果
- 修复所有构建错误
- 确保所有使用 React Hooks 的组件都有正确的 'use client' 标识
- 环境变量配置完整，满足应用运行需求