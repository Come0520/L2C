## 修复计划

### 问题分析
1. **字段名不匹配**：`points_transactions` 表中关联积分账户的字段是 `points_account_id`，但代码中使用了 `account_id`
2. **接口定义需要确认**：需要检查 `PointsAccount` 接口是否与数据库字段一致

### 修复步骤

#### 1. 修改 `PointsAccount` 接口定义
- 文件：`slideboard-frontend/src/types/points.ts`
- 检查并修改 `PointsAccount` 接口，确保字段与数据库一致

#### 2. 修复积分交易查询条件
- 文件：`slideboard-frontend/src/services/points.client.ts`
- 第50行：将 `eq('account_id', account.id)` 修改为 `eq('points_account_id', account.id)`

### 预期结果
- 积分交易历史查询能够正常工作
- 避免因字段名不匹配导致的运行时查询失败
- 确保代码与数据库结构保持一致

### 验证方法
- 运行前端应用，测试积分交易历史功能
- 检查浏览器控制台是否有查询错误
- 验证积分交易数据能够正确显示