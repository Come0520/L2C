# 完成剩余错误清除工作

## 修复计划

### 1. 修复 `any` 类型错误（8 处）

#### 1.1 wallpaper-strategy.test.ts (6 处)

创建 `WallpaperCalcParams` 接口，替换所有 `as any`：

```typescript
interface WallpaperCalcParams {
    width?: number;
    height?: number;
    fabricWidth?: number;
    unitPrice?: number;
    calcType?: string;
    rollLength?: number;
    widthLoss?: number;
    cutLoss?: number;
    wallSegments?: Array<{ width: number }>;
}
```

#### 1.2 po-lifecycle.test.ts (2 处)

创建 `TransactionContext` 接口，替换 `as any`：

```typescript
interface TransactionContext {
    query: {
        arStatements: {
            findMany: vi.Mock;
        };
        purchaseOrders: {
            findMany: vi.Mock;
        };
    };
    insert: {
        arStatements: vi.Mock;
    };
}
```

#### 1.3 compare-utils.ts (2 处)

将 `DiffField<any>` 改为 `DiffField<unknown>`：

```typescript
changes: Record<string, DiffField<unknown>>;
```

### 2. 修复 react/no-unescaped-entities 错误（2 处）

将引号 `"` 替换为 `&quot;`：

```typescript
"做小边"优化阈值 (cm) - 节省超过此值建议做小边
```

改为：

```typescript
&quot;做小边&quot;优化阈值 (cm) - 节省超过此值建议做小边
```

### 3. 可选：修复未使用变量警告

删除未使用的变量声明（不影响功能）
