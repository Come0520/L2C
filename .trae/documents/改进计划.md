## 改进计划

### 1. UI一致性优化 (`slideboard-frontend/src/app/(auth)/login/page.tsx`)
- **优化内容**：将登录页短信验证码输入的图标从 Mail 改为短信/验证码相关图标
- **修改位置**：第304行
- **预期效果**：避免用户误解为邮箱登录，保持UI一致性

### 2. 管理页面文案统一 (`slideboard-frontend/src/app/system/page.tsx`)
- **优化内容**：系统用户页移除“邮箱”列或替换为“手机号”
- **修改位置**：第115-122行（表头）和第128行（表格内容）
- **预期效果**：与项目规则“不使用邮箱”保持一致

### 3. 测试验证
- 运行类型检查 (`npm run typecheck`)
- 运行 lint 检查 (`npm run lint`)
- 运行测试 (`npm test`)
- 运行构建命令 (`npm run build`)

### 实施顺序
1. 首先进行UI一致性优化，修改登录页短信验证码图标
2. 然后进行管理页面文案统一，修改系统用户页
3. 最后进行测试验证

### 预期结果
- 登录页短信验证码输入图标改为短信/验证码相关图标
- 系统用户页不再显示“邮箱”列，改为显示“手机号”列
- 所有测试通过
- 项目符合“不使用邮箱”的规则

### 验收标准
- 代码库不再出现邮箱登录/注册/重置密码方法及测试
- 登录页仅展示手机号登录路径
- 登录页短信验证码输入图标不再是Mail图标
- 系统用户页不再显示邮箱列
- npm run lint、npm run typecheck、npm run test:ci全部通过