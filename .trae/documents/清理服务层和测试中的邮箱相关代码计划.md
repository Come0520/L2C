## 清理服务层和测试中的邮箱相关代码计划

### 1. 服务层清理 (`slideboard-frontend/src/services/auth.client.ts`)
- **移除 `loginWithEmail` 方法** (第48-67行)
- **移除 `registerWithEmail` 方法** (第109-135行)
- **移除 `resetPasswordForEmail` 方法** (第215-228行)
- **更新 `updateUser` 方法**，移除 `email` 参数，不允许通过该方法更新邮箱

### 2. 测试文件清理 (`slideboard-frontend/src/services/__tests__/auth-client.test.ts`)
- **移除 `loginWithEmail` 测试套件** (第65-96行)
- **移除 `registerWithEmail` 测试套件** (第294-314行)
- **移除 `resetPasswordForEmail` 测试套件** (第372-407行)
- 检查其他测试中是否有使用邮箱相关的逻辑，确保全部清理

### 3. 检查其他依赖文件
- 搜索代码库，确保没有其他文件调用了这些邮箱相关的方法
- 检查是否有其他地方依赖了这些邮箱方法

### 4. 测试验证
- 运行测试 (`npm test`)，确保移除邮箱相关代码后测试仍然通过
- 运行类型检查 (`npm run typecheck`)，确保没有类型错误
- 运行 lint 检查 (`npm run lint`)，确保代码质量
- 运行构建命令 (`npm run build`)，确保项目可以正常构建

### 实施顺序
1. 首先清理服务层的邮箱相关方法
2. 然后清理测试文件中的邮箱相关测试
3. 接着检查并清理其他依赖文件
4. 最后进行测试验证

### 预期结果
- 服务层不再包含任何邮箱相关的方法
- 测试文件不再包含任何邮箱相关的测试
- 所有邮箱相关代码被完全清理
- 项目构建成功，没有任何错误
- 所有测试通过

### 代码清理要点
- 确保完全移除所有邮箱相关的方法和测试
- 不保留任何邮箱相关的代码片段
- 确保服务层只提供手机号相关的认证方法
- 确保测试只覆盖手机号相关的认证功能
- 遵循项目规则"不使用邮箱"的要求