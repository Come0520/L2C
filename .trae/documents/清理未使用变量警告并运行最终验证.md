# 清理未使用变量警告并运行最终验证

## 任务概述
跳过修复 any 类型错误，专注于清理代码中的未使用变量警告，然后运行最终验证。

## 具体步骤

### 1. 清理未使用变量警告（55 个）
- 删除未使用的变量声明（如 `setTodos`、`setAlerts`、`setSalesUsers`、`setIsLoading` 等）
- 移除未使用的 eslint-disable 指令
- 优化代码，减少冗余

### 2. 运行最终验证
- 运行 `pnpm type-check` 确保类型检查通过
- 运行 `pnpm lint` 查看最终的错误和警告数量

## 预期结果
- 减少 lint 警告数量（从 65 个减少到更少）
- 确保类型检查通过
- 提供最终的 lint 报告