# 清除剩余错误计划

## 剩余错误分析

### 1. `any` 类型错误（8 处）- 测试文件
这些错误在测试文件中，通常可以接受，但可以添加类型注释或使用更具体的类型：

**受影响文件：**
- `src/features/quotes/calc-strategies/__tests__/wallpaper-strategy.test.ts` (6 处)
- `src/features/supply-chain/__tests__/po-lifecycle.test.ts` (2 处)

**修复方案：**
- 添加 `// eslint-disable-next-line @typescript-eslint/no-explicit-any` 注释
- 或者创建适当的类型接口

### 2. react/no-unescaped-entities 错误（2 处）
需要转义引号字符：

**受影响文件：**
- `src/features/settings/components/curtain-calc-settings.tsx` (2 处)

**修复方案：**
- 将 `"做小边"` 改为 `&quot;做小边&quot;`
- 将 `"做小边"` 改为 `&quot;做小边&quot;`

### 3. 警告（不影响代码运行）
这些警告不会阻止代码运行，可以暂时忽略或后续优化。

## 具体修改

1. **wallpaper-strategy.test.ts**
   - 在 6 处 `as any` 前添加 `// eslint-disable-next-line @typescript-eslint/no-explicit-any`

2. **po-lifecycle.test.ts**
   - 在 2 处 `as any` 前添加 `// eslint-disable-next-line @typescript-eslint/no-explicit-any`

3. **curtain-calc-settings.tsx**
   - 将第 144 行的引号转义为 HTML 实体