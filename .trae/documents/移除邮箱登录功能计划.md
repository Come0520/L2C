## 移除邮箱登录功能实施计划

### 1. 登录页面修改 (`slideboard-frontend/src/app/(auth)/login/page.tsx`)
- 移除 `useEmail` 状态变量和相关切换逻辑
- 删除邮箱/手机号切换按钮 UI 组件
- 移除密码登录中的邮箱分支逻辑
- 清理邮箱输入字段相关代码
- 移除邮箱相关的表单验证逻辑

### 2. AuthContext 修改 (`slideboard-frontend/src/contexts/auth-context.tsx`)
- 更新 `login` 函数，移除 `isEmail` 参数，仅支持手机号登录
- 修改 `register` 函数，移除 `email` 参数，调整 supabase 注册逻辑
- 更新类型定义，移除邮箱相关的参数

### 3. 注册页面修改 (`slideboard-frontend/src/app/(auth)/register/page.tsx`)
- 移除表单中的 `email` 字段
- 删除邮箱相关的验证逻辑
- 更新 `formData` 状态，移除 `email` 属性
- 修改提交逻辑，仅传递手机号相关参数

### 4. 确保 Supabase 配置
- 确保 Supabase Auth 配置中禁用邮箱登录
- 检查并更新 Supabase 策略，确保不依赖邮箱字段

### 5. 测试验证
- 验证登录页仅显示手机号登录选项
- 测试手机号+密码登录流程
- 测试手机号+验证码登录流程
- 测试注册流程
- 确保所有相关功能正常工作

### 实施顺序
1. 首先修改 AuthContext，移除邮箱相关的核心逻辑
2. 然后修改登录页面，移除邮箱登录 UI 和逻辑
3. 接着修改注册页面，移除邮箱注册功能
4. 最后进行测试验证

### 预期结果
- 登录页无邮箱登录入口
- 注册页无邮箱输入字段
- 所有邮箱相关代码被完全清理
- 手机号登录流程测试通过
- 注册流程测试通过

### 代码清理要点
- 移除所有 `LoginMethod.EMAIL` 相关代码
- 清理邮箱输入字段和验证逻辑
- 统一登录文案与操作流
- 确保 AuthContext 不支持邮箱登录方法
- 移除所有与邮箱相关的导入和依赖