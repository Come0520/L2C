## 线索板块引用路径检查与试运行验证计划

### 1. 已完成的检查

#### 路径配置检查
- ✅ 项目使用Next.js框架，配置了`@/*`路径别名，指向`./src/*`
- ✅ `tsconfig.json`中路径配置正确：`"@/*": ["./src/*"]`
- ✅ 所有组件引用均使用了`@/*`路径别名，符合规范

#### 组件结构检查
- ✅ 线索板块主要页面：`/src/app/leads/page.tsx`
- ✅ 组件结构清晰，符合功能模块划分原则
- ✅ 关键组件存在且引用路径正确：
  - `LeadsPageHeader`：`@/features/leads/components/dashboard/LeadsPageHeader`
  - `LeadTable`：`@/features/leads/components/list/LeadTable`
  - `useLeads`：`@/hooks/useLeads`
  - 其他组件引用均正确

### 2. 待验证的内容

#### 运行时验证
1. 运行TypeScript类型检查，确保无类型错误
2. 运行ESLint检查，确保代码符合规范
3. 启动开发服务器，验证线索板块页面能正常访问
4. 检查动态导入组件是否能正常加载
5. 验证页面路由跳转是否正常

### 3. 实施步骤

1. **类型检查**：
   ```bash
   npm run type-check
   ```

2. **代码规范检查**：
   ```bash
   npm run lint
   ```

3. **启动开发服务器**：
   ```bash
   npm run dev
   ```

4. **访问验证**：
   - 访问 `http://localhost:3000/leads` 检查列表视图
   - 访问 `http://localhost:3000/leads/kanban` 检查看板视图
   - 访问 `http://localhost:3000/leads/analytics` 检查转化分析

5. **组件功能验证**：
   - 检查页面头部组件是否正常渲染
   - 检查预约日历组件是否正常加载
   - 检查统计卡片是否正常显示
   - 检查线索列表是否能正常渲染

### 4. 预期结果

- ✅ TypeScript类型检查无错误
- ✅ ESLint检查无错误
- ✅ 开发服务器成功启动
- ✅ 线索板块各页面能正常访问
- ✅ 所有组件能正常加载和渲染
- ✅ 路由跳转正常

### 5. 风险与应对

- **依赖缺失**：如遇依赖缺失，运行 `npm install` 安装依赖
- **配置错误**：检查 `next.config.js` 和 `tsconfig.json` 配置
- **组件加载失败**：检查组件导出方式和引用路径
- **路由错误**：检查 `app` 目录结构是否符合Next.js路由规范

### 6. 验证完成后

- 总结验证结果
- 修复发现的问题
- 确保线索板块符合最佳实践要求

通过以上计划，我们可以全面验证线索板块的引用路径正确性和运行通畅性。