## 重构计划

### 1. 引入 Toast 组件
- 在文件顶部引入项目使用的 Toast hook：`import { toast } from '@/components/ui/toast'`

### 2. 替换通知逻辑
当前文件中没有直接使用 `alert()`，但在删除模板功能中缺少成功和失败的通知。需要：
- 在 `handleDelete` 函数的 `try` 块中添加删除成功的 Toast 通知
- 在 `catch` 块中添加删除失败的 Toast 通知

### 3. 具体修改点
- **引入语句**：在第 10 行后添加 `import { toast } from '@/components/ui/toast'`
- **成功通知**：在 `await deleteTemplate(id)` 后添加 `toast.success('模板删除成功')`
- **失败通知**：在 `catch` 块中添加 `toast.error(error.message || '模板删除失败')`

### 4. 移除调试代码
检查文件中是否有调试用的 `alert()`，如有则直接删除

### 5. 确保不依赖 alert 的阻断特性
原代码中使用 `confirm()` 进行确认，不会被替换，因此不存在依赖 alert 阻断特性的问题

### 预期效果
- 删除模板成功时，显示绿色的成功 Toast 通知
- 删除模板失败时，显示红色的错误 Toast 通知
- 通知不会阻断代码执行，提供更好的用户体验
- 保持与项目其他部分一致的通知风格