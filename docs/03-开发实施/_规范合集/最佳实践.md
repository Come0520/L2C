# Slideboard Frontend 最佳实践实施工作计划

本计划基于 **VibeCoding 敏捷 5S 规则** 及 **Next.js 16 (App Router) 最佳实践** 制定，旨在解决当前项目中存在的命名不规范、模块重复及结构臃肿问题。

> **Next.js 16 特别提示**：
> 1. **Server Actions**: 在业务逻辑下沉时，优先使用 Server Actions 替代 API Routes。
> 2. **React Compiler**: 保持组件纯粹，避免不必要的 `useMemo/useCallback`，充分利用 React 19 的自动优化。
> 3. **Client Boundary**: 严格分离 Server/Client 组件，`page.tsx` 默认为 Server Component。

## 📅 阶段一：核心模块整治 (Critical Cleanup)
**目标**：消除代码重复，统一 "Single Source of Truth"。
**优先级**：P0 (最高)

### 1. 合并 Installation 模块
目前存在 `src/features/installation` (单数) 和 `src/features/installations` (复数)，需统一合并至 **复数** 目录。

#### 待迁移文件清单
| 原路径 (src/features/installation/...) | 目标路径 (src/features/installations/...) | 说明 |
| :--- | :--- | :--- |
| `components/installation-assign-form.tsx` | `components/form/InstallationAssignForm.tsx` | 迁移并重命名 |
| `components/installation-detail.tsx` | `components/detail/InstallationDetail.tsx` | 需检查与 `InstallationDetailDrawer` 的复用关系 |
| `components/installation-task-list.tsx` | `components/list/InstallationTaskList.tsx` | 迁移并重命名 |
| `schemas/installation-schema.ts` | `schemas/installation.ts` | 统一命名风格 |
| `types/index.ts` | `types/index.ts` | 合并类型定义 |

#### 执行步骤
1.  移动文件到目标目录。
2.  全局搜索并更新引用：
    *   搜索：`from '@/features/installation/`
    *   替换为：`from '@/features/installations/`
3.  删除 `src/features/installation` 目录。

---

## 📅 阶段二：命名规范标准化 (Naming Convention)
**目标**：严格执行 "组件文件 PascalCase" 规则。
**优先级**：P1 (高)

### 1. Auth 模块 (`src/features/auth/components`)
*   `login-form.tsx` -> `LoginForm.tsx`
*   `login-view.tsx` -> `LoginView.tsx`
*   `register-form.tsx` -> `RegisterForm.tsx`
*   `register-view.tsx` -> `RegisterView.tsx`
*   `sms-login-form.tsx` -> `SmsLoginForm.tsx`
*   `social-login.tsx` -> `SocialLogin.tsx`

### 2. Dashboard 模块 (`src/features/dashboard/components`)
*   `activity-item.tsx` -> `ActivityItem.tsx`
*   `activity-list.tsx` -> `ActivityList.tsx`
*   `stats-card.tsx` -> `StatsCard.tsx`
*   `task-item.tsx` -> `TaskItem.tsx`
*   `task-list.tsx` -> `TaskList.tsx`
*   `todo-card.tsx` -> `TodoCard.tsx`
*   `todo-categories.tsx` -> `TodoCategories.tsx`

### 3. Editor 模块 (`src/features/editor/components`)
*   `create-slide-modal.tsx` -> `CreateSlideModal.tsx`
*   `properties-panel.tsx` -> `PropertiesPanel.tsx`
*   `slide-editor.tsx` -> `SlideEditor.tsx`
*   `slide-thumbnail-list.tsx` -> `SlideThumbnailList.tsx`
*   `toolbar.tsx` -> `Toolbar.tsx`

### 4. Leads 模块 (`src/features/leads/components`)
*   `dashboard/appointment-reminders.tsx` -> `dashboard/AppointmentReminders.tsx`
*   `dashboard/lead-stats-cards.tsx` -> `dashboard/LeadStatsCards.tsx`
*   `dashboard/leads-page-header.tsx` -> `dashboard/LeadsPageHeader.tsx`
*   `dedupe/lead-dedupe-dialog.tsx` -> `dedupe/LeadDedupeDialog.tsx`
*   `detail/lead-detail-drawer.tsx` -> `detail/LeadDetailDrawer.tsx`
*   `detail/lead-timeline.tsx` -> `detail/LeadTimeline.tsx`
*   `import/lead-import-dialog.tsx` -> `import/LeadImportDialog.tsx`
*   `list/lead-filters.tsx` -> `list/LeadFilters.tsx`
*   `list/lead-table.tsx` -> `list/LeadTable.tsx`
*   *(根目录下文件)*:
    *   `appointment-calendar.tsx` -> `AppointmentCalendar.tsx`
    *   `assignment-dialog.tsx` -> `AssignmentDialog.tsx`
    *   `business-tag.tsx` -> `BusinessTag.tsx`
    *   `confirm-tracking-dialog.tsx` -> `ConfirmTrackingDialog.tsx`
    *   `create-lead-dialog.tsx` -> `CreateLeadDialog.tsx`
    *   `follow-up-dialog.tsx` -> `FollowUpDialog.tsx`
    *   `lead-action-buttons.tsx` -> `LeadActionButtons.tsx`
    *   `lead-status-badge.tsx` -> `LeadStatusBadge.tsx`
    *   `lead-tags-input.tsx` -> `LeadTagsInput.tsx`
    *   `quote-details.tsx` -> `QuoteDetails.tsx`
    *   `tag-selector.tsx` -> `TagSelector.tsx`

### 5. UI 组件库 (`src/components/ui`)
虽然 shadcn/ui 默认使用 kebab-case，但为了项目统一，建议迁移（可选，但推荐）：
*   `batch-action-bar.tsx` -> `BatchActionBar.tsx`
*   `gooey-theme-toggle.tsx` -> `GooeyThemeToggle.tsx`
*   `moving-border-card.tsx` -> `MovingBorderCard.tsx`
*   `paper-button.tsx` -> `PaperButton.tsx`
*   ... (其余 UI 组件同理)

---

## 📅 阶段三：架构瘦身 (Architecture Refactoring)
**目标**：App Router (`src/app`) 仅保留路由定义，业务逻辑下沉至 Features。
**优先级**：P2 (中)

### 1. Finance 模块迁移
*   **当前位置**：`src/app/finance` 可能包含非 page.tsx 的组件文件。
*   **目标位置**：`src/features/finance`
*   **操作**：
    1.  创建 `src/features/finance/components`。
    2.  创建 `src/features/finance/hooks`。
    3.  将页面中的复杂 UI 拆分为组件放入 feature 目录。

### 2. Academy 模块迁移
*   **当前位置**：`src/app/academy`
*   **目标位置**：`src/features/academy`
*   **操作**：将知识库展示逻辑、系统介绍组件移入 features。

---

## 📅 阶段四：依赖与环境清理 (Environment Cleanup)
**目标**：保持依赖纯净，提升构建速度。
**优先级**：P3 (持续)

### 1. 依赖检查
*   **执行**：运行 `npm run clean:unused` (基于 knip)。
*   **处理**：移除未使用的 npm 包。

### 2. 配置文件
*   **检查**：`next.config.js`。
*   **优化**：确认 `turbo` 配置是否开启，以提升开发体验。

---

## 📝 执行检查清单 (Checklist)

- [x] **Phase 1**: 完成 `src/features/installation` 到 `installations` 的合并。
- [x] **Phase 1**: 修正所有相关引用路径。
- [ ] **Phase 2**: 完成 `src/features/auth` 组件重命名。
- [ ] **Phase 2**: 完成 `src/features/dashboard` 组件重命名。
- [ ] **Phase 2**: 完成 `src/features/leads` 组件重命名。
- [ ] **Phase 2**: 完成 `src/features/products` 组件重命名。
- [ ] **Phase 2**: 完成 `src/features/orders` 组件重命名。
- [ ] **Phase 3**: 建立 `src/features/finance` 结构并迁移代码。
- [ ] **Verification**: 运行 `npm run build` 确保无构建错误。
- [ ] **Verification**: 运行 `npm run lint` 确保无格式错误。
