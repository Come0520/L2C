# 已完成事项列表 (Completed Tasks)

> 以下是从 docs/todolist.md 中移出的已解决任务。

---

## 🔴 P0 · 紧急

### [x] [PERF-001] Finance 报表页 Server Component 串行 DB 查询性能优化

**发现时间**：2026-03-11  
**发现场景**：E2E 第二批 Chromium 测试，finance-smoke / finance-ar-advanced 页面加载超时

**修复方案**：

1. 确认使用了 `unstable_cache(fn, key, { revalidate: 300 })` 包装 `getIncomeStatementData`、`getBalanceSheetData`、`getCashFlowData`
2. 用 `<Suspense>` 包裹数据渲染部分，骨架屏先输出
3. E2E 测试侧增加 `page.waitForFunction(() => !document.querySelector('[data-loading=\"true\"]'))` 作为兜底

### [x] [PERF-002] Approval 设计器页 force-dynamic 串行 Server Action 优化

**发现时间**：2026-03-11  
**发现场景**：E2E 第二批 approval-designer 测试，导致 10 条后续用例 did not run

**修复方案**：

1. 改串行为并行：`await Promise.all([getMyQuoteConfig(), getApprovalFlows()])`
2. 将 `force-dynamic` 改为 `revalidate: 60`
3. `approval-designer.spec.ts` 中的 `timeout: 60000` 防御性放宽至 `90000`

---

## 🟠 P1 · 重要

### [x] [E2E-001] finance-ar 应收账单页面 retry#1 仍超时

**发现时间**：2026-03-11  
**原因**：finance-ar 页面 Server 数据查询较慢，E2E 的 safeGoto 60s 超时不够用  
**修复详情**：

1. 确认 `finance-ar/page.tsx` 中已经正确使用了 `<Suspense>`
2. 将 E2E 脚本 `finance-ar.spec.ts` 与 `finance-ar-advanced.spec.ts` 中的所有超时参数调高至 120000

### [x] [E2E-B3-001~004] after-sales-liability 售后定责相关页面加载超时

**发现时间**：2026-03-11（E2E 第三批）  
**修复方案**：

1. 抽取 `getAfterSalesTicketDetailInternal` 服务端查询核心，并加入 `unstable_cache`。
2. 将详情页包裹上带 `[data-loading]` 属性的 `Skeleton`，与 Suspense 配合，客户端接入 `initialData` 防闪。
3. 在测试里补上了 `waitForDetailLoad(page)`。

### [x] [E2E-B3-005~006] install-fee-calculation 等安装单详情相关的加载超时

**发现时间**：2026-03-11（E2E 第三批）  
**修复方案**：

1. `getInstallWorkersAction` 加入了 `unstable_cache` 缓存支持。
2. 安装单详情页从串行渲染改为使用 `Promise.all` 的并行渲染。
3. 为页面包裹带 `[data-loading]` 的 Suspense 骨架屏组件。
4. E2E 用例端侧配套加入 `waitForDetailLoad(page)` 在点击并加载 URL 后执行防阻塞兜底。
