<!--
  客户列表页
  pages/crm/index.wxml
  审计修复: 添加下拉刷新、loading 状态、上拉加载
-->
<view class="container">
  <!-- 搜索栏 -->
  <view class="search-bar">
    <view class="search-input-wrap">
        <icon type="search" size="14" color="#94a3b8"></icon>
        <input 
            class="search-input" 
            placeholder="搜索姓名或手机号" 
            confirm-type="search"
            bindinput="onSearchInput" 
            bindconfirm="onSearch"
        />
    </view>
  </view>

  <!-- 列表（含下拉刷新 + 上拉加载） -->
  <scroll-view 
    class="crm-list" 
    scroll-y="true"
    refresher-enabled
    refresher-triggered="{{refreshing}}"
    bindrefresherrefresh="onPullDownRefresh"
    bindscrolltolower="onLoadMore"
  >
    <!-- Loading 状态 -->
    <view wx:if="{{loading && !list.length}}" class="loading-state">
      <text>加载中...</text>
    </view>

    <!-- 列表内容 -->
    <block wx:for="{{list}}" wx:key="id">
        <view class="crm-card" bindtap="onTapItem" data-id="{{item.id}}">
            <view class="card-header">
                <view class="name-row">
                    <text class="name">{{item.name}}</text>
                    <view class="tags" wx:if="{{item.tags}}">
                        <text class="tag">{{item.tags}}</text>
                    </view>
                </view>
                <view class="call-btn" catchtap="onCall" data-phone="{{item.phone}}">
                    📞
                </view>
            </view>
            
            <view class="card-body">
                <view class="info-row">
                    <text class="label">手机：</text>
                    <text class="value">{{item.phone}}</text>
                </view>
                <view class="info-row">
                    <text class="label">地址：</text>
                    <text class="value">{{item.address || '未填写'}}</text>
                </view>
                <view class="info-row" wx:if="{{userInfo.role === 'admin'}}">
                    <text class="label">归属：</text>
                    <text class="value highlight">{{item.salesRepName}}</text>
                </view>
            </view>
        </view>
    </block>

    <!-- 加载更多 -->
    <view class="load-more" wx:if="{{loadingMore}}">
      <text>加载更多...</text>
    </view>
    <view class="load-more" wx:if="{{noMore && list.length}}">
      <text>— 没有更多了 —</text>
    </view>

    <!-- 空状态 -->
    <view class="empty-state" wx:if="{{!list.length && !loading}}">
        <image class="empty-img" src="/assets/empty.png" /> 
        <text>暂无客户数据</text>
    </view>
  </scroll-view>

  <!-- FAB 按钮 -->
  <view class="fab-add" bindtap="onAdd" hover-class="fab-active">
    +
  </view>
</view>
