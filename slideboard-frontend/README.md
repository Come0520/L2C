# Slideboard - 现代化幻灯片展示平台

基于Next.js 15+构建的现代化幻灯片展示和管理平台，专注于提供流畅的演示体验和协作功能。

## 🚀 功能特性

- **📱 手机号注册登录** - 无需邮箱，简单快捷
- **🎨 可视化编辑器** - 拖拽式幻灯片编辑
- **🎭 丰富模板库** - 多种预设模板选择
- **👥 实时协作** - 多人同时编辑和评论
- **📊 演示模式** - 全屏演示和演讲者模式
- **🔄 云端同步** - 自动保存和版本管理
- **📱 响应式设计** - 支持多设备访问

## 🛠️ 技术栈

- **前端**: Next.js 15+, React 18, TypeScript, Tailwind CSS v4
- **后端**: Supabase (PostgreSQL, Realtime, Auth, Storage)

### 🎨 样式开发指南 (Tailwind CSS v4)

本项目已迁移至 **Tailwind CSS v4**，请遵循以下新规范：

- **配置方式**: 样式配置已从 `tailwind.config.js` 迁移至 `src/app/globals.css` 中的 `@theme` 指令。
- **颜色系统**: 使用 **OKLCH** 色彩空间 (P3 Gamut)，颜色更鲜艳。
  - 示例: `bg-primary-500` (实际对应 `oklch(0.623 0.214 259.815)`)
- **容器查询**: 推荐使用内置容器查询替代视口媒体查询。
  - `class="@container"`: 标记容器
  - `class="@md:grid-cols-2"`: 基于容器宽度的断点 (注意使用 `@` 前缀)
- **动态值**: 支持任意值的动态工具类，无需配置。
  - 示例: `grid-cols-15`, `w-17`, `opacity-33`
- **认证**: Supabase Auth
- **实时**: Supabase Realtime
- **存储**: Supabase Storage

## 📦 快速开始

### 环境要求

- Node.js 18+
- npm 或 yarn

### 安装依赖

```bash
cd slideboard-frontend
npm install
```

### 环境配置

1. 复制环境变量文件：
```bash
cp .env.example .env.local
```

2. 配置Supabase：
   - 在 `.env.local` 中填写你的 Supabase URL 和 anon key
   - 确保数据库表结构已创建（参考技术架构文档）

### 开发运行

```bash
npm run dev
```

访问 http://localhost:3000

## 📁 项目结构

```
slideboard-frontend/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── page.tsx           # 首页
│   │   ├── auth/              # 认证页面
│   │   ├── editor/            # 编辑器页面
│   │   ├── present/           # 演示页面
│   │   └── layout.tsx         # 根布局
│   ├── components/            # 组件
│   │   ├── header.tsx         # 顶部导航
│   │   ├── slide-editor.tsx   # 幻灯片编辑器
│   │   ├── toolbar.tsx        # 工具栏
│   │   └── ...
│   ├── contexts/              # React Context
│   │   └── auth-context.tsx   # 认证上下文
│   └── lib/                   # 工具库
│       └── supabase.ts        # Supabase客户端
├── public/                    # 静态资源
└── ...
```

## 🔧 开发指南

### 命名规范

严格按照项目命名规范执行：
- 变量: camelCase
- 常量: SCREAMING_SNAKE_CASE  
- 函数: camelCase
- 类: PascalCase
- 数据库: snake_case
- API路径: kebab-case

### 代码规范

- 使用TypeScript严格模式
- 遵循ESLint和Prettier配置
- 组件使用函数组件和Hooks
- 状态管理使用Context API

### Git提交规范

```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 样式调整
refactor: 代码重构
test: 测试相关
chore: 构建过程或辅助工具的变动
```

## 🚀 部署

### 构建生产版本

```bash
npm run build
```

### 启动生产服务器

```bash
npm start
```

### Docker部署

```dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --only=production

COPY . .
RUN npm run build

EXPOSE 3000

CMD ["npm", "start"]
```

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交Issue和Pull Request！

## 📞 联系我们

- 邮箱: support@slideboard.com
- 官网: https://slideboard.com