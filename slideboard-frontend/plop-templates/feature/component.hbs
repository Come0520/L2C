// src/features/{{name}}/components/{{properCase name}}Card.tsx
/**
 * {{properCase name}} Card Component
 * @module features/{{name}}/components
 */

import React from 'react';
import { {{properCase name}} } from '../types';

interface {{properCase name}}CardProps {
  data: {{properCase name}};
  onEdit?: (id: string) => void;
  onDelete?: (id: string) => void;
}

/**
 * {{properCase name}}Card Component
 * Displays a {{properCase name}} item with actions
 */
export const {{properCase name}}Card: React.FC<{{properCase name}}CardProps> = ({
  data,
  onEdit,
  onDelete,
}) => {
  return (
    <div className="bg-white rounded-lg shadow-md p-4 hover:shadow-lg transition-shadow">
      <div className="flex justify-between items-start mb-3">
        <h3 className="text-lg font-semibold text-gray-800">{data.name}</h3>
        <div className="flex gap-2">
          {onEdit && (
            <button
              onClick={() => onEdit(data.id)}
              className="text-blue-600 hover:text-blue-800"
            >
              Edit
            </button>
          )}
          {onDelete && (
            <button
              onClick={() => onDelete(data.id)}
              className="text-red-600 hover:text-red-800"
            >
              Delete
            </button>
          )}
        </div>
      </div>
      <div className="text-sm text-gray-500">
        <p>Created: {new Date(data.createdAt).toLocaleDateString()}</p>
        <p>Updated: {new Date(data.updatedAt).toLocaleDateString()}</p>
      </div>
    </div>
  );
};
