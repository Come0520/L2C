'use client';

import React from 'react';

export function CurtainFabricSpecsFields() {
    return (
        <div className="p-4 border rounded-md bg-muted/50">
            <p className="text-muted-foreground">窗帘规格字段在恢复模式下暂不可用。</p>
            <p className="text-sm text-muted-foreground">(Curtain specs fields are not available in recovery mode.)</p>
        </div>
    );
}
