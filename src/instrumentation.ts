export async function register() {
    // 当需要初始化 OpenTelemetry 或进行首次运行设置时，可在这里实现。
    // 注意：此方法是 Next.js 应用启动时（或每个冷启动实例的第一条请求时）运行的 Hook
    // 若使用 node 环境下的 OpenTelemetry 这里通常执行 require('initalize-otel')
}
